__pycache__/
*.py[cod]
node_modules/
*.egg-info/
build/
dist/
.pytest_cache/
.council/
# /council used to write its scratch files here; it writes them to a temp dir now, and
# this stays only so an older copy of the command cannot dirty the tree.
.council-*.md
.DS_Store
.claude/settings.local.json
# TypeScript incremental build state
web/tsconfig.tsbuildinfo
